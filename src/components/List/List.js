import React, { Component, useState, useEffect } from 'react';

const List = (props) => {
    const { items } = props;
    return <div className="list">
        List
    </div>
};

export default List;